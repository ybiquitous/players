{
  "name": "players",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:ybiquitous/players.git",
  "engines": {
    "node": "7"
  },
  "scripts": {
    "dev": "nodemon -r dotenv/config -i pages/ server.js",
    "build": "next build",
    "start": "node -r dotenv/config server.js",
    "test": "NODE_ENV=test ava --serial",
    "test:watch": "yarn test -- --watch",
    "postinstall": "if [ \"$NODE_ENV\" = \"production\" ]; then yarn build && yarn db:sync; fi",
    "lint-staged": "lint-staged",
    "lint": "eslint --ignore-path=.gitignore .",
    "db:sync": "node -e \"require('./models').sequelize.sync()\"",
    "db:drop": "node -e \"require('./models').sequelize.drop()\""
  },
  "lint-staged": {
    "*.@(js|jsx)": [
      "eslint --fix --max-warnings=0",
      "git add"
    ]
  },
  "pre-commit": {
    "run": "lint-staged"
  },
  "dependencies": {
    "body-parser": "*",
    "dotenv": "*",
    "express": "",
    "isomorphic-fetch": "*",
    "material-ui": "*",
    "next": "*",
    "pg": "*",
    "pg-hstore": "*",
    "react": "*",
    "react-dom": "*",
    "react-tap-event-plugin": "*",
    "sequelize": "*",
    "sequelize-cli": "*"
  },
  "devDependencies": {
    "ava": "*",
    "eslint": "*",
    "eslint-config-ybiquitous": "*",
    "lint-staged": "*",
    "nodemon": "*",
    "pre-commit": "*"
  }
}
